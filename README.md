# Emotion Classification Dashboard

5 Emotion Classification Dashboard with model builder using Flask.

## Installation

Install with pip:

`$ pip install -r requirements.txt`

## Run Program

Run it inside venv

`venv/Scripts/active`

Then,

`flask run`
